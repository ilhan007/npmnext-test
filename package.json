{
  "description": "",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ilhan007/npmnext-sample.git"
  },
  "scripts": {
    "build": "npm-run-all --sequential build:main build:fiori",
    "build:main": "cd packages/main && yarn build",
    "build:fiori": "cd packages/fiori && yarn build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.3"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/main",
    "packages/fiori"
  ]
}
